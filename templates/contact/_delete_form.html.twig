<form method="post" action="{{ path('app_contact_delete', {'id': contact|raw.id}) }}" onsubmit="return confirm('{{ 'Global.Are you sure you want to delete this item'|trans|raw }}');" style="display:inline">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact|raw.id) }}">
	<button class="btn btn-link text-decoration-none text-orange p-0">
		{{ 'Btn.Delete'|trans|raw }}
	</button>
</form>
