<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg bg-white sticky-top shadow-sm mb-4">
	<div class="container">
		<a class="navbar-brand fw-bold" href="{{ path('app_dashboard') }}">
			<img src="{{ asset('assets/img/logo/logo-horizontal.svg') }}" alt="informatique.it.com" class="brand-logo d-inline-block align-text-top">
		</a>
		<button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div id="navMain" class="collapse navbar-collapse">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item ms-lg-2 mb-2">
					<a class="ms-auto mb-2 btn w-100 {{ currentRoute starts with 'app_user_' ? 'btn-orange' : 'btn-outline-orange' }}" href="{{ path('app_user_index') }}">
						<i class="fa-regular fa-users"></i>
						{{ 'Nav.Users'|trans|raw }}
					</a>
				</li>
				{% if is_granted('ROLE_SUPER_ADMIN') %}
					<li class="nav-item ms-lg-2 mb-2">
						<a class="ms-auto mb-2 btn w-100 {{ currentRoute starts with 'app_company_' ? 'btn-orange' : 'btn-outline-orange' }}" href="{{ path('app_company_index') }}">
							<i class="fa-regular fa-building"></i>
							{{ 'Nav.Companies'|trans|raw }}
						</a>
					</li>
				{% endif %}
				<li class="nav-item ms-lg-2 mb-2">
					<a class="btn btn-outline-secondary w-100" href="{{ path('app_logout') }}">
						<i class="fa-regular fa-arrow-right-from-bracket"></i>
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
